#include <iostream>
#include <vector>
#include <string>

using namespace std;

#define all(v) v.begin(), v.end()
#define rall(v) v.rbegin(), v.rend()
#define pb push_back
#define mp make_pair

typedef long long int llong;
typedef unsigned long long int ullong;
typedef unsigned long int ulong;
typedef unsigned int uint;

int main(){
	std::ios::sync_with_stdio(false);

	int t, n;
	int i, j, k;

	cin >> t;

	while(t--){
		cin >> in_str;
		n = in_str.length();

		vector<vector<int> > small(26, vector<int>(n, -1));
		vector<vector<int> > big(26, vector<int>(n, -1));
	}

	return 0;
}
